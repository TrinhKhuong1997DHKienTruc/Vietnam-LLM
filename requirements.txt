# Core runtime
transformers==4.44.2
safetensors==0.4.5
accelerate==1.3.0
huggingface-hub==0.25.2

# PyTorch: install separately for your OS/CUDA. Fallback to CPU wheel if needed.
# For Linux CPU-only, pip will resolve a cpu wheel for torch 2.5.x on Python 3.13
# For Windows, use pip default (CPU) or install CUDA build from pytorch.org
# We do not pin torch hard to allow platform-specific resolution
torch>=2.5.0

# Optional: fast generation
optimum==1.21.4

# CLI utilities
rich==13.8.1