# Core ML/LLM stack
transformers>=4.44.2
accelerate>=0.34.2
safetensors>=0.4.5
# Torch: pin to a modern release with Python 3.13 wheels
# For CUDA users, install the appropriate CUDA build if needed (see README)
torch==2.5.1

# Data and utils
numpy>=1.26.0
pandas>=2.2.0
matplotlib>=3.8.0
seaborn>=0.13.0
requests>=2.31.0
tqdm>=4.66.0
python-dateutil>=2.8.2

# Finance data
yfinance>=0.2.50
pandas_market_calendars>=4.4.1

# Optional but often required by HF
sentencepiece>=0.2.0

# Windows terminal colors (optional)
colorama>=0.4.6
