# Core ML and utilities
numpy>=1.24
pandas>=2.0
matplotlib>=3.7

# Time series forecasting with Chronos-Bolt
# Chronos library (official) - installed from GitHub
# Note: Requires git to be available on the system
git+https://github.com/amazon-science/chronos-forecasting.git

# Hugging Face + PyTorch
transformers>=4.41
# Torch CPU by default; users with CUDA can install a CUDA-specific wheel
# (The plain 'torch' package will install a CPU build by default on Windows/Linux.)
torch>=2.2

# Data source
yfinance>=0.2.40